package com.gmail.at.pukanito.model.container

import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class GraphPathTest extends FunSpec with ShouldMatchers  {

  describe("GraphPath") {

    it("should be possible to represent a root item") (pending)

    it("should be possible to represent an absolute path") (pending)

    it("should be possible to represent a relative path") (pending)

    it("should be possible to add a relative path to a relative path giving a new relative path") (pending)

    it("should be possible to add a relative path to an absolute path giving an absolute path") (pending)

  }

}