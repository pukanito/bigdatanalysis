package com.gmail.at.pukanito.view.metadata.attributedefinition

import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class CreateAttributeDefinitionTest extends FunSpec with ShouldMatchers {

  describe("CreateAttributeDefinition") {

    it("should create a new attribute definition when no attribute definition with the same id exists") (pending)

    it("should not create a new attribute definition when an attribute definition with the same id exists") (pending)

  }
}
