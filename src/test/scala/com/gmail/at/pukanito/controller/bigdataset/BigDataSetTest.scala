package com.gmail.at.pukanito.controller.bigdataset

import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class BigDataSetTest extends FunSpec with ShouldMatchers {

  describe("BigDataSet") {

    it("should be possible to associate an attribute model with the data set") (pending)

    it("should be possible to not associate an attribute model with the data set") (pending)

    it("should be possible to create data without an attribute model") (pending)

    it("should be possible to create data with an attribute model") (pending)

    it("should be possible to update data without an attribute model") (pending)

    it("should be possible to update data with an attribute model") (pending)

    it("should be possible to delete data") (pending)

    it("should be possible to read data") (pending)

  }

}
