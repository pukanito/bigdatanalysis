package com.gmail.at.pukanito.controller.bigdataset

import org.scalatest.FunSpec
import org.scalatest.matchers.ShouldMatchers

class BigDataModelTest extends FunSpec with ShouldMatchers {

  describe("BigDataModel") {

    it("should be possible to add attribute definitions") (pending)

    it("should be possible to update attribute definitions") (pending)

    it("should be possible to delete attribute definitions") (pending)

    it("should be possible to read attribute definitions") (pending)

    it("should be possible to load the big data model") (pending)

    it("should be possible to save the big data model") (pending)

    it("should be possible to import another big data model") (pending)

  }

}